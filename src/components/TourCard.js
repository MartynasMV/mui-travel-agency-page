import { Paper } from '@mui/material';
import { Grid } from '@mui/material';

const TourCard = () => {
  return (
    <Grid item xs={3}>
      <Paper elevation={3}>Hey</Paper>
    </Grid>
  );
};

export default TourCard;
